export default function ConfigPage() {
  return (
    <div className="mx-auto max-w-2xl">
      <h1 className="font-display text-3xl font-semibold">Configuración</h1>
      <p className="mt-2 text-sm text-white/55">
        TODO: editor de Settings (siteName, tagline, WhatsApp, redes). El modelo ya existe
        en Prisma como <code className="rounded bg-black/30 px-1">Settings</code>. Próxima
        iteración.
      </p>
    </div>
  );
}
